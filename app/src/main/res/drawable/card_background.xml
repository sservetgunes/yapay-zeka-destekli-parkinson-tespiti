<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners android:radius="8dp" /> <!-- Kenar yuvarlama miktarı -->
    <solid android:color="@android:color/white" /> <!-- Arka plan rengi -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Kenarlık rengi -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- İçeriğin kenar boşlukları -->
</shape>